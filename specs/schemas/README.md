# Schema Boundary

## Status

Reserved for Todo 2. No placeholder schema is published because a decorative or incomplete contract would create false compatibility. Todo 2 will generate and validate the canonical JSON Schemas from strict typed domain models.
